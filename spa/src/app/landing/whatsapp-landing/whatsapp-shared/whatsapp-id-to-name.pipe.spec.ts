import { WhatsappIdToNamePipe } from './whatsapp-id-to-name.pipe';

describe('WhatsappIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new WhatsappIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
