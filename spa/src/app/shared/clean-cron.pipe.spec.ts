import { CleanCronPipe } from './clean-cron.pipe';

describe('CleanCronPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanCronPipe();
    expect(pipe).toBeTruthy();
  });
});
