import { NaturalCronPipe } from './natural-cron.pipe';

describe('NaturalCronPipe', () => {
  it('create an instance', () => {
    const pipe = new NaturalCronPipe();
    expect(pipe).toBeTruthy();
  });
});
