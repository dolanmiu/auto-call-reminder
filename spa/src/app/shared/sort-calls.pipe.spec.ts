import { SortCallsPipe } from './sort-calls.pipe';

describe('SortCallsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCallsPipe();
    expect(pipe).toBeTruthy();
  });
});
